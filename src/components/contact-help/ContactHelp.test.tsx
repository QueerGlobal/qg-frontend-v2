import React from 'react';
import { render } from '@testing-library/react';
import ContactHelp from './ContactHelp';

describe('Contact Help Component', () => {
  it('should render correctly', () => {
    render(<ContactHelp />);
  });
});
