import React, { FC } from "react";

/**
 * Remember: A user likes a business so the business likes are saved in their user data
 * 
 * @returns 
 */
const Profile: FC = () => {
	return (
		<div>
			<h1>profile</h1>
		</div>
	);
};

export default Profile;
